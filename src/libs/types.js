import ptBr from "./translations/pt-BR.js";

/**
 * @typedef {typeof ptBr} Translation
/* @typedef {'DEFAULT'|'SHOW_SAVED_HIGHLIGHTS'} PopupMode
 */

// Make this file a module. But it should be imported only for its types.
export {};
